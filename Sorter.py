"""
@author: Angus Whitehead
"""


class Sorter:

    def sort_height(self,pokedex,direction):
        sorted_items= []
        counter = 0
        for name in pokedex.items():
            if counter == 0:
                sorted_items[counter] = name
            else:
                if pokedex[name]:


    def sort_weight(self,pokedex,direction):
        sorted_items = []
        for name in pokedex.items():

